Vico: Broadcasts issues and recommended fixes to chat
-- if fix accepted - applies fix
-- otherwise does nothing

LLM support:
-- assumes Ollama is already present in the base image
-- handles hive-facing LLM lifecycle and prompt interactions
-- chat actions: LLM_START, LLM_STOP, LLM_PULL, LLM_STATUS, LLM_PROMPT




